//UseEffect Hook: The useEffect hook is used to perform side effects in functional components, like fetching data or updating the DOM after a render.

useEffect(()=>{
    //Fetch data here or perform side effects
},[]); //empty array means it runs only once, like componentDidMount